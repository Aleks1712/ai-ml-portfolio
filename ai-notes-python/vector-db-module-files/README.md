# Vector DB module files (cleaned)

This folder contains the raw course notebooks/data, reorganized for GitHub readability.

## Folders

- `pinecone/`: Pinecone intro notebooks
- `vector-store/`: vector store basics (embedding, preprocessing, index)
- `semantic-search/`: semantic search notebooks
- `data/`: CSV files used by the notebooks

